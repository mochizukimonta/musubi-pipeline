# セキュリティ上の問題の報告について

## 報告の方法

**公開 Issue には書かないでください。**
[Security Advisory](https://github.com/mochizukimonta/musubi-pipeline/security/advisories/new)
から非公開で報告してください。GitHub アカウントがあれば誰でも使えます。

書いていただけると助かること:

- 何が起きるか(どんな影響があるか)
- 再現手順、または該当するコードの場所
- 影響するバージョン
- 攻撃者に必要な前提(同じ同期グループのメンバーである必要があるか、
  ネットワーク上の位置、など)

**個人の環境情報は最小限にしてください。**
デバイスID・ホスト名・実際のプロジェクトフォルダのパスは、
再現に必要でなければ含めないでください。

## 対応の目安

個人が趣味の範囲で開発しているため、企業のような SLA はありません。
目安として:

| | |
|---|---|
| 受領の返信 | 1週間以内 |
| 影響評価の返信 | 2週間以内 |
| 修正の公開 | 深刻度に応じて。データ消失や意図しないコード実行につながるものは最優先 |

返信がない場合は Advisory にコメントを追加してください。見落としの可能性があります。

## サポート対象

最新のリリースのみです。古いバージョンへのバックポートは行いません。

## このアドオンで特に見てほしいところ

構造上、次の部分が攻撃面になります。報告の際の参考にしてください。

- **同期フォルダから読むもの全般。** P2P で共有されるフォルダは
  「チームの他の誰かが書き込めるフォルダ」です。そこに置かれた JSON・
  ファイル名・`.blend` は信頼できない入力として扱う設計になっています。
  検証を通り抜ける経路があれば、それは脆弱性です
- **パスの取り扱い。** `core.safe_path()` が全書き込みの門番で、
  プロジェクトルート外への書き込み(シンボリックリンク経由を含む)を
  拒否します。ここを迂回できる経路
- **Syncthing バイナリの取得。** 公式 GitHub リリースから HTTPS で取得し、
  同リリースの `sha256sum.txt.asc` と照合してから展開します。
  取得元はホスト名のホワイトリストで固定しています。zip 展開時のパス検証
  (Zip Slip 対策)も含みます
- **HTML ボードの生成。** `board_html.py` はユーザー入力を
  すべて `html.escape` します。エスケープ漏れは注入経路です
- **REST API の待ち受け。** `127.0.0.1` のみで、API キーは同期対象外の
  ローカルフォルダに置きます

設計の全体像は [セキュリティ設計](docs/ja/セキュリティ設計.md) にあります。

## 脆弱性ではないもの

以下は既知の前提です。報告は不要です。

- **Syncthing デバイスID は秘密ではありません。** 公開鍵のハッシュで、
  招待ファイルに含まれます。これを知られてもフォルダの中身は読めません
  (参加には必ずリーダーの承認操作が必要です)
- **ロックはアドバイザリです。** OS レベルの排他ではないので、
  アドオンを使わずに直接ファイルを開けば編集できます。事故を防ぐ仕組みで、
  悪意ある相手を止める仕組みではありません
- **Syncthing 自体の脆弱性。**
  [Syncthing プロジェクト](https://github.com/syncthing/syncthing/security)へ
  報告してください
- **Blender の「Python 自動実行」を有効にしている場合の任意コード実行。**
  同期されてきた `.blend` に仕込まれたスクリプトが動きます。
  これは Blender の仕様で、アドオンのセキュリティ監査が
  CRITICAL で警告する項目です。無効にしてください
