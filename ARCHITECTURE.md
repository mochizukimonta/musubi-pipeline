# アーキテクチャ

このアドオンがなぜこの形をしているか、という文書です。ファイル単位の
詳細は [コード地図](docs/ja/コード地図.md) にあります。

## 出発点にある判断

**同期プロトコルを自作しない。** 暗号化・鍵交換・NAT越え・競合解決を
独自実装すれば、脆弱性とデータ消失がほぼ確実に入ります。転送は
[Syncthing](https://syncthing.net/) に委任し、このアドオンは
**検証役**に徹します。

この線引きが構造の大半を決めています。アドオンの仕事は次の3つだけです。

1. Syncthing の面倒な導入を自動化する(取得・設定・起動・共有・承認)
2. 同期の結果が本当に正しいかを暗号学的ハッシュで検証する
3. その上に制作運用の層(履歴・ロック・進行・品質)を載せる

## 3層構造

```
┌─ UI層 ─────────────────────────────────────────┐
│ ui.py                    パネル描画のみ。I/O 禁止   │
├─ オペレーター層(bpy 依存)──────────────────────┤
│ ops.py  st_ops.py  task_ops.py  review_ops.py     │
│ ver_ops.py  quality_ops.py  spec_ops.py  reel.py   │
├─ データ層(bpy 非依存 = Blender 外でも動く)───────┤
│ core.py  sync.py  tasks.py  reviews.py             │
│ versions.py  spec.py  render_profiles.py           │
│ syncthing.py  board_html.py                        │
│ security.py      … bpy は任意参照                  │
│ quality.py       … 例外。bpy.data を読む(検査のため)│
└────────────────────────────────────────────────┘
```

境界は形式ではなく実利のためにあります。

**データ層が `bpy` を import しないので、Blender を起動せずにテストできます。**
だから CI は素の GitHub Actions ランナーで回り、98テストが2秒で終わります。
Blender をインストールする必要がありません。同じ性質のおかげで、Blender が
起動しない状況でもシステムの Python から同期設定を読んで修理できます。

**UI層で I/O をしません。** `draw()` は毎フレーム呼ばれるため、ここで
ディスクやネットワークに触ると Blender の操作感が落ちます。

## ディスク上のデータ設計

すべてのデータは `<プロジェクト>/.musubi/` 配下の JSON です。

```
.musubi/
├── project.json            プロジェクト共通情報(同期ID)
├── spec.json               レンダー仕様(本番/仮の2プロファイル)
├── board.html              進捗ボード。同期しない(後述)
├── manifest_<端末>.json     SHA-256 同期照合リスト
├── env/env_<端末>.json      各端末の Blender/Python バージョン
├── status/<シーン>_<カット>.json   進捗ステータス
├── reviews/<シーン>_<カット>/<時刻>_<端末>.json
├── deps/<シーン>_<カット>.json     このカットのリンク先
└── versions/<相対パス>/<時刻>_<端末>.blend + .json
```

### 共有インデックスを持たない

**これが全体を貫く最重要の設計原則です。** 「1件 = 1ファイル、一覧は
フォルダ走査で構築」という形を全機能で徹底しています。ステータス一覧・
レビュー一覧・バージョン履歴のどれにも、まとめて書き込まれる索引ファイルが
ありません。

理由は P2P 同期です。索引ファイルを1つ置くと、2人が同時に何かを更新した
瞬間にその1ファイルへの書き込みが衝突し、Syncthing が
`.sync-conflict-...` を作ります。**索引がなければ衝突する対象が存在しません。**
走査のコストを払って、競合をゼロにする取引です。

同じ理由で、ファイル名に必ず端末名が入ります(`manifest_<端末>.json`、
`<時刻>_<端末>.blend`)。別の端末が同じ名前を書くことがありません。

### レビューは追記のみ

レビューコメントは1件1ファイルで、編集も削除もできません。不変ログを
前提にしているので、同時書き込みで絶対に衝突しません。「コメントを
取り消したい」は「取り消しコメントを追加する」で表現します。

### 書き込みは必ず2つの関門を通る

- `core.safe_path()` — プロジェクトルート外を指すパス(シンボリックリンクを
  辿った先も含む)を `PipelineError` にします。**全書き込みの門番です**
- `core.atomic_replace()` — 一時ファイルへ書いてから置換します。
  Windows では同期ツールが直後のファイルを走査のため一瞬開いていて
  `PermissionError` になるので、リトライ付きです

## Blender へのフックは2つだけ

`load_post`(開いた時)と `save_post`(保存した時)。これだけです。

**常駐タイマー・常駐スレッドを持ちません。** 同期状態の取得は
「ファイルを開いた時 / 保存した時 / ボタンを押した時」に、1回きりの
短命なバックグラウンドスレッドで行います。結果の画面反映は、取得完了までの
間だけ生きるタイマーが行い、終わったら自動で消滅します。

`Ctrl+S` が特別なのは、`save_post` ハンドラが「プロジェクト内の全保存」を
パイプライン保存(自動履歴 + ロック + 進捗更新)に変えているからです。
ルートフォルダを設定したこと自体をパイプライン管理への同意と見なす設計で、
専用ボタンを知らなくても正しく管理されます。履歴退避はバックグラウンドに
逃がしてあり、**保存そのものを遅らせません**。

## 同期フォルダを信頼しない

P2P で共有されるフォルダは「チームの他の誰かが書き込めるフォルダ」です。
そこから読むものは信頼できない入力として扱います。

- **`.blend` の Python 自動実行**が最大のリスクです。Blender の
  「Auto Run Python Scripts」が有効だと、同期されてきたファイルに仕込まれた
  コードが動きます。セキュリティ監査が CRITICAL で警告します
- **実行形式の混入検出。** `.exe` `.py` `.bat` はもちろん、
  `.html` `.svg` もブラウザで開くとスクリプトが動くため実行形式に準じて扱います
- **`board.html` は同期しません。** `.stignore` で除外し、各端末が
  ローカル生成したものだけを開く運用にしています。HTML を同期で配ると、
  悪意あるピアが差し替えたものを他の人のブラウザで開かせる経路になります
- **HTML に入れる文字列はすべて `html.escape` を通します**
- **外から読む JSON は形式検証と長さ制限を付けます**

## 外部通信は2か所だけ

- `github.com` / `api.github.com` / `objects.githubusercontent.com` —
  Syncthing バイナリの初回取得のみ。ホワイトリスト固定で、同リリースの
  `sha256sum.txt.asc` と SHA-256 照合してから展開します
  (zip 展開時のパス検証 = Zip Slip 対策つき)
- `127.0.0.1` — Syncthing のローカル制御 REST API。API キーは
  **同期対象外**のローカルフォルダに置きます

`syncthing.py` の `ALLOWED_HOSTS` がこの境界です。ここに機能を足すときは
この2か所という制約を壊さないでください。

詳細は [セキュリティ設計](docs/ja/セキュリティ設計.md) にあります。

## レジストリで広がる設計

いくつかの機能は「1行足すだけで全体に反映される」形にしてあります。
機能追加のときは、まずここに足せないか考えてください。

| 足す場所 | 自動で反映される先 |
|---|---|
| `tasks.STATUSES` | ボードUI・状態変更メニュー・HTMLボード・集計 |
| `reviews.VERDICTS` | 判定メニュー・ステータス連動 |
| `render_profiles.SETTINGS` | 仕様の取込・適用・差分チェックリスト・品質チェック・HTMLボードの対照表 |
| `security.audit()` に1関数 | 監査結果の表示(レベルで自動色分け) |
| `core.TEMPLATES` | フォルダ構造テンプレートのメニューとUI |

`render_profiles.py` が分かりやすい例です。各項目を
`(グループ, 表示名, getter, setter)` として定義してあるので、
同じ定義から「シーンから取り込む」「シーンへ適用する」「差分を出す」の
3つが導けます。

## 横断ルール

コードを触るときはどこでも守ってください。

1. プロジェクト内への書き込みは `safe_path` で検証し `atomic_replace` で書く
2. データ層に `bpy` を持ち込まない
3. 共有インデックスファイルを作らない
4. 常駐処理を足さない(タイマー・スレッドはイベント駆動で短命のみ)
5. ルート外のパスは `PipelineError`(生の `ValueError` を漏らさない)
6. HTML に入れる文字列はエスケープ、外から読む JSON は検証と長さ制限
7. 新しい `.py` には SPDX ヘッダーを付ける

## Blender 4.x / 5.x 両対応

`blender_version_min = 4.2` なので、両系列で動く書き方をします。
API が変わった箇所は `hasattr` で分岐しています。

- 出力形式: 5.x は `image_settings.media_type`(`'VIDEO'` 等)が必要
- アニメーション: 5.x はレイヤードアクション
  (`action.layers[].strips[].channelbags[].fcurves`)
- シーケンサー: 5.x は `sequence_editor.strips`。空のコレクションが
  falsy になる罠があります
- アイコン名: 5.x で廃止されたものがあります。既に使っている名前から選ぶのが安全

## リポジトリの構成

```
musubi-pipeline/            git リポジトリのルート
├── musubi_pipeline/        ここが配布zipの中身になる
│   ├── blender_manifest.toml
│   ├── LICENSE             GPL全文(配布物に同梱するため)
│   └── *.py                21モジュール
├── tests/                  zipの外。配布されない
├── pytest.ini
├── docs/ja/
└── .github/workflows/
```

manifest がパッケージフォルダの中にあるのは意図的です。Blender は
`blender_manifest.toml` のあるフォルダを zip 化するので、この配置なら
`tests/` と `docs/` は自動的に配布物から外れます。同時に
`from musubi_pipeline import core` というパッケージ import が成立するので、
テストがそのまま動きます。
